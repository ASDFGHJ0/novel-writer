# 墨屿 · 小说人物关系网

> 一个纯原生 JavaScript + Canvas 2D 的小说辅助写作工具，重点解决“人物关系网”的自动提取、可视化和手动编辑问题。

## 一、项目背景

`novel-writer` 是一个协助小说创作的本地 Web 工具。项目已经具备：

- 分卷、分章管理小说文本
- 左侧作品树，右侧角色 / 笔记面板
- 正文编辑器、章节搜索、目标统计等基础写作功能
- 独立的人物关系网界面，用于观察小说中的角色和关系

人物关系网最初采用“自动共现”策略：扫描正文段落，如果两个角色名字同时出现在同一段落，就认为他们之间有一条关系边，并按共现次数计算关系强度。

这种自动方式适合快速形成“底图线索”，但无法表达用户脑中的真实设定，例如方向、关系类型、章节时间范围等，因此项目进入“双轨制”升级阶段。

## 二、技术栈

- 原生 JavaScript，无 React / Vue 等框架
- HTML5 Canvas 2D 绘制关系网
- CSS 自定义属性实现亮色 / 暗色主题
- `ResizeObserver` 自适应画布尺寸
- Pointer Events 处理拖动、点击、滚轮缩放
- 手写力导向布局算法
- `localStorage` 持久化项目数据

核心文件：

- `app.js`：应用主逻辑，数据模型、章节编辑、导出导入、事件绑定
- `graph.js`：人物关系网模块，数据提取、物理布局、Canvas 渲染、交互
- `styles.css`：整体样式
- `index.html`：入口页面

## 三、当前架构

人物关系网模块 `graph.js` 主要分为三层。

### 1. 数据提取（`extract`）

- 从 `window.Moyu.getProject()` 获取项目数据
- 节点来自 `project.chars`
- 自动边来自段落共现统计
- 关系强度分级：擦肩（>=1）、频繁（>=4）、紧密（>=8）
- 通过正则 `SPEECH_RE` 和停用词表发现疑似新人物

### 2. 力导向布局（`makeSim` + `step`）

- 节点对象：`{ x, y, vx, vy, r, fixed }`
- 初始位置按圆周均匀分布
- 节点间有排斥力，防止重叠
- 有关系的节点间有弹簧吸引力
- 中心引力防止节点飞出
- 边界约束、最大速度限制、速度阻尼

### 3. Canvas 渲染（`draw`）

- 每帧 `requestAnimationFrame` 清屏重绘
- 按关系强度绘制不同粗细和颜色的边
- 节点颜色按主角 / 配角 / 反派 / 其他区分
- 节点内部显示名字前两字，下方显示完整名字
- 选中节点时淡化无关节点和边

### 4. 交互（`bindCanvas`）

- 拖动节点
- 点击节点选中并显示右侧关系
- 滚轮缩放
- 右侧人物列表点击高亮

## 四、遇到的问题与修复

### 问题 1：人物超过 6 个后关系网严重形变，无法看到全部人物

原因：

```javascript
const f = (d - rest) * 0.025;
dx /= d; dy /= d;
e.a.vx += dx * f * d; e.a.vy += dy * f * d;
e.b.vx -= dx * f * d; e.b.vy -= dy * f * d;
```

`dx/dy` 已经归一化成单位向量，但代码又乘了一次距离 `d`，导致吸引力变成 `(d - rest) * d`，随距离二次增长。节点一多，图被暴力拉塌并挤到画布边缘。

修复：

```javascript
e.a.vx += dx * f; e.a.vy += dy * f;
e.b.vx -= dx * f; e.b.vy -= dy * f;
```

同时优化了初始圆周布局，并给排斥力、最大速度做了调整。

### 问题 2：高 DPI 屏幕出现拖影和重影，人物看起来像几百个叠在一起

原因：

```javascript
ctx.clearRect(0, 0, r.width, r.height);
```

`r.width/r.height` 是 CSS 像素，而 Canvas 内部尺寸已经按 `devicePixelRatio` 放大，导致高 DPI 屏幕下画布没有被完整清除，旧帧不断叠加。

修复：

```javascript
ctx.clearRect(0, 0, canvas.width, canvas.height);
```

### 问题 3：人物多了之后拖动困难，节点之间像锁在一起

原因：

- 吸引力在远距离拉得过猛
- 排斥力不足以压住吸附
- 拖动一个节点时，相连节点被边持续拉扯，整张图像刚体一样移动

修复：

- 给吸引力加 3 的封顶
- 提高近距离排斥力
- 减弱中心引力
- 拖动节点时引入 `calm` 系数，把整体物理力度降到 35%

## 五、双轨制升级需求（PRD）

在保留原有“自动共现边”作为底图线索的基础上，增加一套“用户手动定义的关系网”。

### 1. 人物节点属性

- 姓名（必填）
- 别名 / 外号（数组）
- 头像（URL 或默认图标）
- 代表色（HEX）
- 阵营：正派 / 邪派 / 中立 / 其他
- 所属势力（如门派、家族）
- 状态：活跃 / 死亡 / 失踪 / 离线

### 2. 关系边属性

- 方向性：单向 / 双向
- 关系类型：亲情 / 爱情 / 师徒 / 上下级 / 结义 / 宿敌 / 利用 / 暗恋等，支持自定义
- 关系描述：一句话概括
- 时间锚点：`[start_chapter, end_chapter]`，空表示全局存在
- 权重 / 强度：1 - 5，控制连线粗细

### 3. 界面布局

- 左侧：结构化操作面板
  - 人物列表：按姓名搜索，按阵营 / 状态过滤
  - 属性编辑器：人物和关系的 CRUD
  - 快捷建边：拖拽人物 A 到人物 B 创建关系
- 右侧：无限画布
  - 滚轮缩放、拖拽平移、框选
  - 三种布局：力导向、层级树状、环形
  - 节点手动微调位置

### 4. 视觉映射

- 节点形状：圆形 = 普通人物，六边形 = 核心 / 特殊人物，菱形 = 势力组织
- 节点颜色：按阵营映射，如红 = 敌、蓝 = 友、灰 = 中立
- 节点层级：主角 / 核心节点加大或发光，边缘节点降低透明度
- 连线类型：实线 = 明面关系，虚线 = 暗中 / 潜伏关系
- 连线方向：单向关系绘制箭头
- 连线粗细：按权重 1 - 5px 映射
- 连线避让：交叉处优先使用贝塞尔曲线

### 5. 交互行为

- 悬停聚焦：鼠标 Hover 节点 A 时，非直接相连的节点和边透明度降至 10% - 20%
- 悬停提示：鼠标 Hover 连线时显示“关系类型 + 关系描述” Tooltip

### 6. 进阶功能

- 时间轴动态演化：底部章节滑动条，按 `[start_chapter, end_chapter]` 过滤关系边
- 视角书签：保存画布中心、缩放比例、可见节点集合，一键切换
- 关系路径查询：A -> C 无直接边时，通过 BFS 找最短路径并高亮
- 导出：当前画布视图导出为高清 PNG，全量数据导出为 JSON 备份

## 六、实施计划与函数修改摘要

### 数据层

- `extract()`：在自动共现边后追加“自动边 + 手动边”合并去重逻辑
- `app.js` 的 `seedProject()`：增加 `manualRels[]`、`bookmarks[]`、`graphSettings`
- `addCharacter()`：支持人物新字段
- 新增 `normalizeEdge()` / `mergeEdges()`

### 模拟层

- `makeSim()`：读取人物新字段，设置阵营颜色、状态过滤、形状
- `step()`：增加 `currentLayout !== 'force'` 时跳过物理
- 新增 `layoutTree()`：按势力 / 上下级做分层 BFS 树状布局
- 新增 `layoutCircular()`：按阵营分区环形布局
- 新增 `applyLayout(mode)`：切换 force / tree / circular

### 渲染层

- `draw()`：
  - 按 `currentChapter` 过滤时间范围外的边
  - 按 `type` 判断实线 / 虚线
  - 按 `direction` 判断是否画箭头
  - 按 `weight` 映射线宽
  - 按 `shape` 绘制圆 / 六边形 / 菱形
  - 按 `camp` 映射颜色
  - 核心节点加发光边框
  - 死亡 / 失踪等状态降低透明度
- 新增 `drawArrow()`、`drawHexagon()`、`drawDiamond()`

### 交互层

- `bindCanvas()`：
  - 追加 `Shift + 拖动` 创建手动边
  - 追加点击边进入编辑
  - 追加空白区域拖拽平移和框选
- 新增 `edgeAt()`、`createEdge()`、`editEdge()`、`deleteEdge()`

### UI 与功能层

- `renderSide()`：支持人物列表、人物属性编辑、关系属性编辑三种状态
- 新增 `renderTimeline()`：章节时间轴
- 新增 `renderBookmarks()`：视角书签管理
- 新增 `findPath()`：BFS 最短路径
- 新增 `exportPNG()`、`exportJSON()`

## 七、实施进度

### 已完成

- 定位并修复“人物超过 6 个后关系网形变”问题
- 修复高 DPI 画布未完整清除导致的拖影问题
- 优化力导向参数，改善多节点聚团和拖动锁死问题
- 扩展 `app.js` 数据模型，增加 `manualRels[]`、`bookmarks[]`、`graphSettings`
- `addCharacter()` 支持人物新字段

### 待完成

- `graph.js` 中手动边与自动边的合并逻辑
- 手动边 CRUD 与 Shift 拖动建边
- 树状 / 环形布局切换
- 时间轴滑动条
- 视角书签
- 关系路径查询
- PNG / JSON 导出
- 左侧属性编辑器和过滤控件
- 相关样式补充

## 八、当前技术结论

1. 力导向算法中，吸引力必须使用线性弹簧形式，不能再次乘以距离。
2. Canvas 清屏必须使用 `canvas.width / canvas.height`，不能使用 CSS 尺寸。
3. 多节点、多边的复杂关系网需要同时考虑：节点初始散开、近距离强排斥、吸引力封顶、速度限制和拖动冷却。
4. 自动共现边只能作为“线索”，手动边才能承载方向、类型、时间等真实设定，因此需要双轨制数据模型。

---

### 核心文件位置

- `C:\Users\gu\novel-writer\app.js`
- `C:\Users\gu\novel-writer\graph.js`
- `C:\Users\gu\novel-writer\styles.css`
- `C:\Users\gu\novel-writer\index.html`

> 文档整理时间：2026-08-21
