# 墨屿 · 项目现状

更新时间：2026-09-03
当前版本：v0.8
项目阶段：本地可用原型 / 可选 AI 人物档案工作流验证

## 产品定位

墨屿是一款纯前端、本地优先的小说写作工具。当前核心亮点不是单独展示人物连线，而是将正文、人物档案、人物关系及其证据连接成可追溯的写作工作流。

## 当前可用功能

### 界面主题

- 明亮、深色、水墨、未来四套主题
- 主题选择自动保存，写作台、资料库和人物关系网同步切换
- 水墨风强调文学感与纸张质感，未来风强调关系数据和状态层级
- 关系网右侧工作区优先展示人物摘要或关系演化，基础编辑折叠保留
- 关系阶段、关联事件和正文证据可同屏查看，并可跳转回正文
- 已提供《潮汐密信》四章完整示范：3名人物、3组关系、10个阶段、4个事件、4条知识与3条伏笔

### 写作工作台

- 多作品、分卷和章节管理
- 章节新增、删除、重命名和拖拽排序
- 正文自动保存、全文搜索、字数与每日目标统计
- 全书简介、章节大纲、角色、设定和灵感面板
- 角色常驻栏使用摘要卡展示身份、阵营、状态、档案完成度和证据数量
- 浅色/深色主题、专注模式和字号调节
- TXT 成稿导出、JSON 项目备份与导入

### 人物关系网

- 根据已确认角色的正文同段共现生成关系线索
- 手动新增、编辑、删除、断开和恢复关系
- 单向/双向、类型、描述、强度和章节范围
- 人物别名、阵营、势力、状态、颜色和形状
- 力导向、树状和环形布局
- 缩放、平移、拖动、筛选、聚焦和路径查询
- 阵营颜色管理、章节时间轴、视角书签
- PNG 图片与 JSON 数据导出

### 故事资料库

- 正文选区标记新角色、绑定已有角色或检索证据
- 使用角色姓名及作者确认的别名精确检索
- 对话、外貌、身份、背景、动机、物品、关系和事件分类
- 正文证据归档、取消归档和原章节跳转
- 完整人物档案及右侧常驻角色卡入口
- 可从角色摘要卡直接打开档案，或进入从正文整理候选区
- 档案候选按外貌、身份、背景、动机、物品、语言风格和核心关系归类
- 候选明确标注事实或推测，可查看原文、忽略或确认写入，不会自动覆盖作者设定
- 关系阶段按起止章节联动关系网时间轴，可关联故事事件与共同正文证据
- 人物知识时间线与“可能提前知情”原文核对
- 规则式人物一致性检查
- 故事事件轴和伏笔状态管理

### 数据安全

- 自动历史快照和手动快照
- 恢复历史版本前保存当前状态
- 章节、分卷、角色、关系、事件和伏笔回收站
- 新旧项目数据字段自动迁移

## 当前文件结构

| 文件 | 用途 |
| --- | --- |
| `index.html` | 唯一页面入口和资源加载顺序 |
| `styles.css` | 写作台与关系网基础样式 |
| `app.js` | 作品、章节、编辑器、侧栏、导入导出和数据桥接 |
| `graph.js` | 人物关系提取、Canvas 渲染与关系网交互 |
| `workspace-tools.js` | 历史、回收站、人物档案、证据、检查、事件和伏笔 |
| `workspace-tools.css` | 故事资料库与输入对话框样式 |
| `README.md` | 面向使用者的项目说明 |
| `示例项目.json` | 关系演化与人物知识演示项目 |
| `.github/workflows/ci.yml` | 公开仓库自动检查 |
| `项目现状.md` | 当前功能、限制和下一步计划 |
| `.gitignore` | 忽略系统生成文件 |

## 已知限制

- 数据保存在当前浏览器的 `localStorage`，没有账号、云同步或多设备同步。
- 清除浏览器站点数据可能导致作品丢失，需要定期导出 JSON。
- 正文证据分类使用本地规则，同一段只给出一个主要分类。
- 档案候选来自姓名或别名命中、同段共现和局部句式规则，不是大模型语义分析；推测项仍需作者判断。
- 一致性检查只提出规则层疑点，不能完整理解文学语境。
- 关系阶段已接入 Canvas 章节时间轴；目前线条颜色仍主要表达强度，阶段类型通过侧栏和悬浮提示呈现。
- 事件和伏笔暂未支持正文选区直接标记与高亮。
- 还没有自动化测试套件和大规模作品性能测试。
- AI 接口目前采用 Chat Completions 兼容格式；浏览器跨域策略取决于用户选择的服务商。
- AI 密钥默认使用 sessionStorage；用户主动勾选长期保存时才写入 localStorage。

## 下一阶段优先级

1. 为关系阶段增加按类型配置线条颜色与变化动画。
2. 为事件和伏笔增加正文选区标记、原文证据和章节跳转。
3. 改进档案候选的句子切分、说话人判断和跨段上下文组合。
4. 将数据存储迁移至 IndexedDB，并提供更可靠的自动备份。
5. 补充核心数据函数测试和浏览器端自动回归。
6. 在证据充分时提供可选的 AI 人物档案草稿，所有结果必须由作者确认。

## 发布检查状态

- JavaScript 语法检查：已通过
- 页面资源引用检查：已通过
- 新项目初始化检查：本轮已修复
- 旧备份导入迁移：本轮已修复
- 角色删除进入回收站：本轮已修复
- 应用内输入框兼容：本轮已统一
- 应用内浏览器核心流程回归：已通过，无控制台错误
- v0.7 角色摘要卡与正文档案候选流程：已通过
- 台词候选说话人过滤：已通过示范项目回归
- v0.8 AI 设置窗口和人物档案生成入口：已通过浏览器回归
